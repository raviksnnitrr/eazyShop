import { EazyShopValidators } from './eazy-shop-validators';

describe('EazyShopValidators', () => {
  it('should create an instance', () => {
    expect(new EazyShopValidators()).toBeTruthy();
  });
});
